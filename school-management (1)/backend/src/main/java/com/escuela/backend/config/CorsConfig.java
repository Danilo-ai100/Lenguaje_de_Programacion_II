package com.escuela.backend.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class CorsConfig {
    // Configuración eliminada. La configuración CORS se gestiona en SecurityConfig.
}
